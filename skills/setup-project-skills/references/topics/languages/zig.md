# Zig

Select only the guide matching the pinned compiler version. Use the exact subtree source below; root discovery does not expose these version guides.

| Skill | Install source | Use when |
| --- | --- | --- |
| `zig-0.15` | [zigcc/skills / zig-0.15](https://github.com/zigcc/skills/tree/main/zig-0.15) | Zig 0.15.x APIs and version-specific migration traps need guidance; verify the installed compiler before using examples. |
| `zig-0.16` | [zigcc/skills / zig-0.16](https://github.com/zigcc/skills/tree/main/zig-0.16) | Zig 0.16.0 APIs and version-specific migration traps need guidance; verify the installed compiler before using examples. |

For a different Zig release, search version-matched sources and verify the installed standard library instead of applying an adjacent version's guide.
